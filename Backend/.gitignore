# Maven
target/
# 保留 Maven Wrapper（mvnw / mvnw.cmd / .mvn/），让克隆方无需预装 Maven

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# 运行时产物
uploads/
logs/
*.log

# 本机专用配置（含数据库密码 / 大模型 Key，绝不提交）
# 用根锚定，只忽略 Backend/config/，不影响 src/.../config/ 这个 Java 包
/config/

# 跨端编辑用的中转目录（临时，勿提交）
.fe_staging/
*.ps1.tmp

# 临时脚本
_*.ps1
